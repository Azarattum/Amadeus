{
  "name": "@amadeus-music/util",
  "type": "module",
  "scripts": {
    "dev": "vitest -c ../../vite.config.js --mode node",
    "coverage": "vitest run -c ../../vite.config.js --coverage --mode node",
    "test": "vitest run -c ../../vite.config.js --mode node"
  }
}

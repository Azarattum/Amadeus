{
  "name": "@amadeus-music/core",
  "version": "0.0.1",
  "type": "module",
  "main": "app.ts",
  "types": "app.ts",
  "scripts": {
    "build": "vite build --emptyOutDir=false",
    "coverage": "vitest run --coverage",
    "dev": "vite-node -w app.ts",
    "preview": "(cd ../../build && node app.cjs)",
    "test": "vitest run"
  },
  "dependencies": {
    "@amadeus-music/util": "workspace:*",
    "libfun": "workspace:*"
  },
  "devDependencies": {
    "vite-node": "0.25.3"
  }
}

{
  "name": "@amadeus/plugin-template",
  "description": "Template for Amadeus plugins.",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --emptyOutDir=false --mode node",
    "coverage": "vitest run --coverage --mode node",
    "test": "vitest run --mode node"
  },
  "dependencies": {
    "@amadeus/core": "workspace:*",
    "@amadeus/util": "workspace:*"
  },
  "devDependencies": {
    "vite": "3.2.3"
  }
}
